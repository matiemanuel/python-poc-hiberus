pipeline {
    agent any
    stages {
        stage("Install dependencies") {
            steps {
                echo 'Install dependencies'
            }
        }

        stage("run tests"){
            echo 'Running tests'
        }

        stage("Reporting results"){
            echo 'Last step'
        }
    }
}