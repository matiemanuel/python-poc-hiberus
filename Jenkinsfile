pipeline {
    agent any
    stages {
        stage("Install dependencies") {
            steps {
                echo 'Install dependencies'
            }
        }

        stage("run tests"){
            steps {
                echo 'Running tests'
            }

        }

        stage("Reporting results"){
            steps {
                echo 'Last step'
            }
        }
    }
}